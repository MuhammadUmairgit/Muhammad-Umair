const allButtons = document.querySelectorAll(".btn");




if() {
    allButtons.forEach(function (singleButton)) {
        singleButton.addEventListner("click",function (event) {
            event.preventDefault();
        })
    }
}